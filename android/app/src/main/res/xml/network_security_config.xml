<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is blocked by default. It is permitted only for the local
  development hosts used by the Android emulator / a machine on the LAN, so
  release builds cannot be downgraded or MITM'd over plain HTTP.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
