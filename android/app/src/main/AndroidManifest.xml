<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- flutter_local_notifications: lets scheduled medication dose reminders
         (zonedSchedule, see lib/core/services/medication_scheduler.dart) be
         re-armed after the device reboots. Inexact scheduling is used, so no
         SCHEDULE_EXACT_ALARM/USE_EXACT_ALARM permission is needed. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- local_auth: required for Face/Fingerprint unlock (Profile > Security). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- geolocator: "sort doctors by distance" needs a coarse fix; FINE is
         declared so Android lets the user pick precise if they prefer. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <application
        android:label="DocGet"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Lets the payment-return page (apps.payments.views.
                 PayriffReturnView on the backend) bounce the external
                 browser straight back into the app after checkout, instead
                 of leaving the user to switch back manually. No Dart-side
                 handling needed: opening this intent alone brings the app
                 to the foreground, which already triggers the existing
                 AppLifecycleState.resumed-based payment/subscription status
                 recheck in PaymentScreen/SubscriptionScreen. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="medalize"/>
            </intent-filter>
            <!-- App Links (QR_SHARE_PROFILE_PLAN.md Phase 3) — opens a shared
                 doctor/hospital profile link straight into the app. Scoped to
                 those two path shapes only, not the whole domain, so a plain
                 link to the medoroapp.com marketing site doesn't also try to
                 launch the app. autoVerify requires medoroapp.com to serve a
                 correct /.well-known/assetlinks.json (see medalize_web) with
                 this app's real release-signing SHA-256 fingerprint, or
                 Android silently falls back to opening the browser instead. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="medoroapp.com"
                    android:pathPattern="/.*/doctor/.*"/>
                <data
                    android:scheme="https"
                    android:host="medoroapp.com"
                    android:pathPattern="/.*/hospital/.*"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- flutter_local_notifications: required for zonedSchedule (medication
             dose reminders) to fire and to survive a device reboot. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
